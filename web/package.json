{
  "name": "ipo-portal-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4321",
    "build": "astro build",
    "cf:preview": "wrangler deploy --dry-run --env=\"\"",
    "cf:deploy": "wrangler deploy --env=\"\"",
    "cf:deploy:production": "wrangler deploy --env production",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@fontsource-variable/inter-tight": "^5.1.1",
    "@fontsource-variable/jetbrains-mono": "^5.1.2",
    "@fontsource-variable/source-serif-4": "^5.1.1",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "wrangler": "^4.94.0"
  }
}
