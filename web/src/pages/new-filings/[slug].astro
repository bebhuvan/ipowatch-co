---
import { listNewFilingPaths } from '../../lib/newFilings';

export function getStaticPaths() {
  return listNewFilingPaths().map(({ slug }) => ({ params: { slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/ipos/${slug}/report/`, 301);
---
